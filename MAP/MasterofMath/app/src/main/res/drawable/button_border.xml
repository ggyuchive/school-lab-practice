<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- 버튼 배경 색상 (투명) -->
    <stroke android:color="#FFFFFF" android:width="2dp" /> <!-- 테두리 색상과 너비 -->
    <corners android:radius="8dp" /> <!-- 테두리의 모서리 반경 (선택 사항) -->
</shape>