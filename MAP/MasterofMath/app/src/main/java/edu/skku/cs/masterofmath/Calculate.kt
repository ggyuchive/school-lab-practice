package edu.skku.cs.masterofmath
/*
fun Calculate(equation: String, type: String): String {
    // Simplify, Derive, TwoDerive, Integrate
    var ret = equation
    if (type == "Derive") {
        ret = GetAPI("derive/$ret")
    }
    else if (type == "TwoDerive") {
        ret = GetAPI("derive/$ret")
        Thread.sleep(100)
        ret = GetAPI("derive/$ret")
    }
    else if (type == "Integrate") {
        ret = GetAPI("area/0:2|$ret")
    }
    Thread.sleep(100)
    ret = GetAPI("simplify/$ret")
    return ret
}

 */